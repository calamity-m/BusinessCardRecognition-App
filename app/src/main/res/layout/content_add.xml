<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/content_add"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="com.example.canberra.businesscardsaver.AddActivity"
    tools:showIn="@layout/activity_add">

    <TextView
        android:text="Tap Capture to turn the Camera on or tap Load to select and image on your phone"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="45dp"
        android:id="@+id/textViewDisplay"
        android:layout_alignParentTop="true"
        android:textAlignment="center"
        android:textSize="22dp"
        android:layout_centerHorizontal="true" />

    <Button
        android:text="Load"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/buttonLoad"
        android:layout_marginEnd="44dp"
        android:layout_alignBaseline="@+id/buttonCaputre"
        android:layout_alignBottom="@+id/buttonCaputre"
        android:onClick="load"
        android:layout_alignParentEnd="true" />

    <Button
        android:text="Capture"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="141dp"
        android:id="@+id/buttonCaputre"
        android:layout_marginStart="45dp"
        android:layout_below="@+id/textViewDisplay"
        android:onClick="capture"
        android:layout_alignParentStart="true" />


</RelativeLayout>
